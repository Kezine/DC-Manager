/* ============================================================================
   Domain `nav` — ENGLISH. Calque EXACT de `../fr/nav.ts` (le français est la
   langue de référence). Libellés des DOMAINES du menu à deux niveaux
   (`app/NavModel` → `NAV_DOMAINS`). Agrégé par `../en.ts`. */
export const nav = {
  domain: {
    inventaire: "Inventory",
    implantation: "Layout",
    reseau: "Network",
    exploitation: "Operations",
    parametres: "Settings",
  },
} as const;
