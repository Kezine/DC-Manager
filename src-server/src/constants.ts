/* Schéma des collections : désormais du code PARTAGÉ front ⇄ back (source de vérité UNIQUE).
   Ce fichier ne fait que ré-exporter `shared/Schema.ts` — fini le « garder en phase » manuel
   avec EntityRegistry / config.ts / Text.ts côté client. Cf. CLAUDE.md « Code partagé ». */
export { Schema } from "../../shared/Schema.js";
